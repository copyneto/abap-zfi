@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_FI_RETPAGTO_SEGMENTOZ {
    grant
        select
            on
                ZC_FI_RETPAGTO_SEGMENTOZ
                    where
                        inheriting conditions from entity ZI_FI_RETPAGTO_SEGMENTOZ;
                        
}