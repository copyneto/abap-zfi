@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_FI_LOG_APV_PGTO {
    grant
        select
            on
                ZC_FI_LOG_APV_PGTO
                    where
                        inheriting conditions from entity ZI_FI_LOG_APV_PGTO;
                        
}