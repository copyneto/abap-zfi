@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_FI_CONTAB_LOG {
    grant
        select
            on
                ZC_FI_CONTAB_LOG
                    where
                        inheriting conditions from entity ZI_FI_CONTAB_LOG;
                        
}