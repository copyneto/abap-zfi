@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_FI_CONTRATO {
    grant
        select
            on
                ZC_FI_CONTRATO
                    where
                        inheriting conditions from entity ZI_FI_CONTRATO;
                        
}