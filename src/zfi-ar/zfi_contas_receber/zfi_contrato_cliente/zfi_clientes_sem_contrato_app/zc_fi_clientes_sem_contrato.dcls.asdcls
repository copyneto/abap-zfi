@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_FI_CLIENTES_SEM_CONTRATO {
    grant
        select
            on
                ZC_FI_CLIENTES_SEM_CONTRATO
                    where
                        inheriting conditions from entity ZI_FI_CLIENTES_SEM_CONTRATO;
                        
}