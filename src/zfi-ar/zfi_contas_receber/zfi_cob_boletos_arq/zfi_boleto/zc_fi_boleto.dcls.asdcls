@EndUserText.label: 'CDS de Autorização'
@MappingRole: true
define role ZC_FI_BOLETO {
    grant
        select
            on
                ZC_FI_BOLETO
                    where
                        inheriting conditions from entity ZI_FI_BOLETO;
                        
}